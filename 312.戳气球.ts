/*
 * @lc app=leetcode.cn id=312 lang=typescript
 *
 * [312] 戳气球
 */

// @lc code=start
enum Flag { left, right }
function maxCoins(nums: number[]): number {
  let flag: Flag = Flag.left
  while (nums.length) {
    let left: number = 1
    let right: number = nums.length - 2
    if (flag) {

    }
  }
};
// @lc code=end

